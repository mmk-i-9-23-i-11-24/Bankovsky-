
def int_to_roman(num):
    # Список значений в порядке убывания
    val = [1000, 900, 500, 400, 100, 90, 50, 40, 10, 9, 5, 4, 1]
    # Соответствующие римские символы
    syms = ['M', 'CM', 'D', 'CD', 'C', 'XC', 'L', 'XL', 'X', 'IX', 'V', 'IV', 'I']
    # Инициализация пустой строки для результата
    roman_num = ''
    # Инициализация индекса для перебора списка значений
    i = 0
    # Пока число больше нуля
    while num > 0:
        # Для каждого раза, когда текущее значение входит в число
        for _ in range(num // val[i]):
            # Добавляем соответствующий римский символ к результату
            roman_num += syms[i]
            # Уменьшаем число на значение, которое мы только что добавили
            num -= val[i]
        # Переходим к следующему значению
        i += 1
    # Возвращаем полученное римское число
    return roman_num

# Принимаем число на вход
n = int(input())
# Проверяем, что число в допустимом диапазоне
if 0 < n < 4000:
    # Выводим результат преобразования
    print(int_to_roman(n))